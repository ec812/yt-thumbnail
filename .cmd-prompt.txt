Read the plan at docs/plans/2026-09-18-yt-thumbnail-generator.md, then implement ALL 8 tasks in order. Build a single index.html with embedded CSS+JS. Key technical details: Canvas size 1280x720. Background image: cover-scale (fill + center crop). Vertical gradient overlay: transparent top to rgba(0,0,0,0.7) bottom. Text: 2-3 lines, gradient-clipped via offscreen canvas + globalCompositeOperation source-in. Font: bold sans-serif ~50px. First line has a checkbox preset to 直播一會. Controls sidebar: file input, 3 text rows, gradient color pickers, overlay opacity slider, font size slider, download button. Export: canvas.toBlob PNG, filename thumbnail-{timestamp}.png. Dark theme UI. After implementing, git init and commit everything.